name: Build iOS Tweak

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Install Theos Framework
      run: |
        git clone --recursive https://github.com/theos/theos.git ~/theos
        echo "THEOS=~/theos" >> $GITHUB_ENV
        
    - name: Setup iOS SDK
      run: |
        mkdir -p ~/theos/sdks
        curl -LO https://github.com/theos/sdks/archive/refs/heads/master.zip
        unzip -q master.zip
        cp -R sdks-master/* ~/theos/sdks/
        rm -rf master.zip sdks-master

    - name: Unzip and Flatten Project Files
      run: |
        python3 -c "
        import zipfile, os, shutil
        
        with zipfile.ZipFile('Drawdylib.zip', 'r') as zip_ref:
            zip_ref.extractall('temp_extract')
        
        src_dir = 'temp_extract'
        if os.path.exists('temp_extract/Drawdylib'):
            src_dir = 'temp_extract/Drawdylib'
            
        for item in os.listdir(src_dir):
            s = os.path.join(src_dir, item)
            d = os.path.join('.', item)
            if os.path.exists(d):
                if os.path.isdir(d):
                    shutil.rmtree(d)
                else:
                    os.remove(d)
            shutil.move(s, d)
            
        shutil.rmtree('temp_extract')
        "
        ls -la

    - name: Compile Tweak Package
      run: |
        export THEOS=~/theos
        make package FINALPACKAGE=1

    - name: Upload Compiled Artifact (.deb / .dylib)
      uses: actions/upload-artifact@v4
      with:
        name: OsirisDrawESP-Build
        path: |
          packages/*.deb
          **/*.deb
